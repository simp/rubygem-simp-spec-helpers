# -*- ruby -*-

require "rubygems"
require 'rake/clean'
require 'find'
require 'rspec/core/rake_task'


@package='simp-spec-helpers'
@rakefile_dir=File.dirname(__FILE__)

require 'simp/spec/rubygem'
Simp::Spec::Rubygem.new(@package, @rakefile_dir)


# vim: syntax=ruby
